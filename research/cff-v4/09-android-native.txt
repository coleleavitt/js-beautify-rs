     2019 IEEE/ACM 41st International Conference on Software Engineering: Companion Proceedings (ICSE-
                                                Companion)



             Deobfuscating Android Native Binary Code
                                 Zeliang Kan1 , Haoyu Wang1 , Lei Wu2 , Yao Guo3 , Guoai Xu1
                   1
                       Beijing University of Posts and Telecommunications 2 PeckShield, Inc. 3 Peking University


    Abstract—In this paper, we propose an automated approach to              Flow (BCF) and Control Flow Flattening (CFF). One key
 facilitate the deobfuscation of Android native binary code. Specif-         feature of our system is that we introduce taint analysis to
 ically, given a native binary obfuscated by Obfuscator-LLVM (the            make semantic level deobfuscation in both general and in-
 most popular native code obfuscator), our deobfuscation system
 is capable of recovering the original Control Flow Graph. To the            struction optimization situation. We also exploit ﬂow-sensitive
 best of our knowledge, it is the ﬁrst work that aims to tackle              symbolic execution to rebuild the seriously obfuscated control
 the problem. We have applied our system in different scenarios,             ﬂow. To overcome the challenge of basic block splitting, we
 and the experimental results demonstrate the effectiveness of our           chopped the original control ﬂow, selected analysis targets
 system based on generic similarity comparison metrics.                      through static features, and dynamically adjust the analysis
                                                                             target sequence to maximize context inheritance.
                        I. I NTRODUCTION
                                                                                Experiments on multiple benchmarks and real-world cases
    Android apps have received widespread adoption in recent                 suggest that our approach is capable of accurately deobfuscat-
 year. Besides the standard Android programming model in                     ing samples obfuscated by O-LLVM. We believe our tool can
 Java, Android Native Development Kit (NDK) is introduced                    be used by security analysts to make it easier to inspect native
 to allow developers to include native code binaries (write in               code, even it is heavily obfuscated.
 C/C++) in their apps.
    Problem: Recent work suggested that native code had been                                  II. A PPROACH OVERVIEW
 widely used in Android apps, which severely complicated the                    Goal. We use the term deobfuscation to refer to the process
 process of static analysis. As Java bytecode can be more easily             of removing the effects of code obfuscation from the native
 decompiled, malware developers usually hide the malicious                   binary, and ideally recover the original code and control ﬂow
 payload and core functionalities in the native code to evade                before obfuscation. For a given APK as input, our system
 detection. Even worse, the native code can be obfuscated,                   ﬁrst extracts the native binary and determines whether it is
 which further increases the difﬁculty of security analysis.                 obfuscated, then analyzes and transforms the code to obtain
 For example, the towelroot exploit (CVE-2014-3153), one of                  a functionally equivalent form that is simpler and easier to
 the the biggest Root Exploits family in Android, was found                  understand. For the non-trivial code, the deobfuscation result
 obfuscated at the native code level by O-LLVM. It took a lot                is rarely the same as the original code, however, it is close to
 of efforts for security researchers to dive into the technical              the original and much easier to understand compared to the
 details of the code due to the obfuscation.                                 obfuscated version.
    Although several attempts have discussed the deobfusca-                     Key Techniques. To address the aforementioned challenges,
 tion of Android apps, all of the them focus on Java-level                   we rely on taint analysis and enhanced symbolic execution
 deobfuscation. For example, DeGuard [3] was proposed to                     to recover O-LLVM obfuscated code. Taint analysis is used
 reverse layout obfuscation (naming obfuscation) generated by                to address the challenge introduced by instruction optimiza-
 ProGuard. Their key idea is to learn a probabilistic model                  tion, which performs a globally feature matching to com-
 over a large number of non-obfuscated apps and to use this                  prehensively detect all obfuscations introduced by O-LLVM
 model to deobfuscate new APKs. Layout obfuscation is the                    and identify instructions needed to be rewrite by tracking
 easiest one in Android app obfuscation, which does not alter                tainted registers. Symbolic execution is used to reconstruct
 the program logic (e.g., control ﬂow) of the apps. To the best              the control ﬂow ruined by Control Flow Flattening (CFF).
 of our knowledge, none of previous studies have attempted to                We ﬁrst identify basic blocks that maintain original operations
 tackle native code deobfuscation yet.                                       based on an ARM-speciﬁc basic block classiﬁcation approach.
    Our approach: In this paper, we propose a novel ap-                      Then we perform chopped symbolic execution to address the
 proach for automated deobfuscation of Android native binary                 path explosion challenge. To perform ﬂow-sensitive symbolic
 code. Technically, our system works by combining static taint               execution, we use a dynamic exchanging model to maximize
 analysis and symbolic execution to remove the obfuscation                   the context inheritance and rebuild the original control ﬂow.
 introduced by different obfuscating techniques in O-LLVM. O-
 LLVM [4] is one of the most widely used code obfuscator for                 A. Instruction Substitution Deobfuscation
 both x86 and ARM platforms. It is implemented as middle-end                   We apply taint analysis to determine the combination of
 passes in the Low Level Virtual Machine (LLVM) compilation                  obfuscating instructions and locate instructions needed to be
 process, which offers guaranteed compatibility with LLVM,                   rewritten. During our analysis, we found that sometimes ob-
 including Instruction Substitution (InsSub), Bogus Control                  fuscated instructions may be mixed with normal instructions.


2574-1934/19/$31.00 ©2019 IEEE                                         322
DOI 10.1109/ICSE-Companion.2019.00135
For example, normal instructions use the registers which                 benchmark [2]. To evaluate our system on real-world Android
obfuscated instructions modiﬁed. The taint analysis we use               apps, we have also identiﬁed 5 Android projects that contain
here guarantees the integrity of the deobfuscation analysis. The         open source C/C++ code, which contain 56 functions in total.
content and address of the deobfuscated instructions that need           We further apply our system to towelroot exploit [1], a binary
to be modiﬁed will be saved temporarily and wait until other             tool used to obtain the root privilege by exploiting privilege
deobfuscating work ﬁnished, before rewritten to the binary.              escalation vulnerabilities. In this case, we could evaluate the
                                                                         effectiveness of our approach on deobfuscating real threats in
B. Bogus Control Flow Deobfuscation                                      the wild.
   We rely on assembly features to detect whether a binary is               InsSub: All the obfuscated operations could be successfully
obfuscated by BCF. When the opaque predicate operation is                recovered in our experiment. The Euclidean distances between
detected in a Basic Block (BB), taint analysis ﬁrst sets the             obfuscated function and original one are in the range from 14
BB as a predicate block and sets operated register as a tainted          to 44 during experiment. After deobfuscation, the distances of
register. Then it will force the conditional jump in the parent          the recovered functions are all below 10.
block reaching the predicate block. After that, because only                BCF: We evaluate our system of BCF deobfuscation on the
one condition jump of the predicate block can actually be                C/C++ obfuscation benchmark and real-world Android apps.
accessed, taint analysis will head to the “alive” branch and             At default, the average CFG similarity between the original
label jump instruction at the end of predicate block as to be            program and the obfuscated program is below 0.4, while it
modiﬁed. Due to the optimization on ARM instructions during              could reach up to 0.870 after deobfuscation.
compilation, sometimes BCF obfuscation can occur in a BB                    CFF: We evaluate our system of CFF deobfuscation on
that relay on a constraint, which is a tainted register compared         three different datasets. At the default obfuscation level, the
to zero (there are some cases that compare with 1), our system           average CFG similarity score is roughly 0.2 after obfuscation,
will also automatically set it as a predicate block. After taint         while our deobfuscation results could achieve a similarity
analysis, all deobfuscated information about instructions that           score of 0.807 on average. After activating basic block split-
needs to be modiﬁed will be used to rebuild the binary.                  ting, similarity scores between the obfuscated and unob-
                                                                         fuscated function are almost all negative correlation, which
C. Control Flow Flattening Deobfuscation                                 indicates that the obfuscated CFG is completely different from
   The CFF obfuscation rebuild the control ﬂow to a SWITCH               the original one. The deobfuscating result of the enhanced CFF
construct. To deobfuscate the control ﬂow, we need to identify           is ranging from 0.72 to 1, and on average is 0.84.
basic blocks that maintain original operations and rebuild the              Full Obfuscation (All 3 Techniques): We further evaluate
control ﬂow. Since the analysis of CFF obfuscated code is                the effectiveness of our approach on recovering the samples
ﬂow sensitive, and also symbolic execution has been proved               obfuscated by all three obfuscation techniques at the same
as an effective program analysis technique that can systemat-            time. The CFG similarity score after obfuscation is -0.144 on
ically explore multiple program paths. It is a means of using            average. However, our system could achieve very good results
symbolic input to analyze a program to determine what inputs             compared with single pass evaluation, with a CFG similarity
cause each part of a program to execute.                                 score above 0.834 after deobfuscation.
   In our approach, our system ﬁrst determines whether the                                     IV. C ONCLUSION
code is obfuscated by CFF. Then static analysis is applied
                                                                            We have presented a novel approach for deobfuscation of
to identify basic blocks that contain original operations. After
                                                                         Android native code based on taint analysis and ﬂow-sensitive
that, we proposed a dynamic exchanging algorithm to promise
                                                                         symbolic execution. Experiments suggested that we could
our system to perform ﬂow sensitive symbolic execution on
                                                                         successfully reverse obfuscations performed by O-LLVM with
these basic blocks, and to reconstruct the original control ﬂow.
                                                                         high accuracy. We believe that our system could become a
The purpose of dynamically exchanging basic blocks in the
                                                                         useful tool for security analysts and researchers to conduct
analysis sequence is to ensure that most of the basic blocks
                                                                         studies including malware detection and program analysis.
inherit the state from the previous analysis before execution,
while not starting the analysis of a basic block from a blank                              ACKNOWLEDGEMENT
state. Also, during the symbolic execution, it is possible to              This work is supported by the National Key Research and
enter other function’s space through the function call. Our              Development Program of China (grant No.2017YFB0801903)
system hooks all possible call instructions. When the symbol             and the National NSF of China (grants No.61702045).
execution encounters hooked addresses, it will automatically                                          R EFERENCES
skip the call instruction and continue the analysis in the
                                                                         [1] CVE-2014-3153. https://github.com/orenl/CVE-2014-3153, 2014.
space of the current function. After being optimized, the ﬁnal           [2] Benchmarks. https://github.com/tum-i22/obfuscation-benchmarks, 2016.
deobfuscation result will be written to the output.                      [3] Benjamin Bichsel, Veselin Raychev, Petar Tsankov, and Martin Vechev.
                                                                             Statistical deobfuscation of android applications. In ACM Sigsac Confer-
                      III. E VALUATION                                       ence on Computer and Communications Security, pages 343–355, 2016.
                                                                         [4] Pascal Junod, Julien Rinaldini, Johan Wehrli, and Julie Michielin.
  We have applied our system to three different datasets. We                 Obfuscator-llvm–software protection for the masses. In International
ﬁrst evaluate our system on a widely used C/C++ obfuscation                  Workshop on Software Protection (SPRO), pages 3–9. IEEE, 2015.




                                                                   323
